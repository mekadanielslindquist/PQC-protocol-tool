# Full install - everything the project uses anywhere (management API,
# PQC/quantum crypto, and the infra/services glue). This is what the
# Dockerfiles install, and it's what you want for a complete local setup.
#
# Split out by layer under requirements/ so you can install less when you
# don't need all of it - e.g. `pip install -r requirements/api.txt` gets
# the management API + dashboard running without compiling numpy/scipy/
# qiskit at all. See requirements/*.txt for what's in each and why.

-r requirements/api.txt
-r requirements/crypto.txt
-r requirements/infra.txt
