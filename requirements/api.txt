# Web/API layer: management_api/ (the FastAPI management service +
# dashboard) and sip_connect/app.py (SIPApp).
#
# This is the lightest install in the project - no numpy/scipy/qiskit
# compilation required - so if you just want the management API and
# test dashboard running (service status, docker checks, key
# *inspection*), this is all you need:
#
#   pip install -r requirements/api.txt
#
# You'll additionally need requirements/crypto.txt for anything that
# actually calls into Falcon/Kyber (key *generation*, HybridSecuritySystem,
# etc) - see GET /crypto/status once the API is running.

-r base.txt

fastapi~=0.115.8
uvicorn~=0.34.0
starlette~=0.45.3
pydantic~=2.10.6
anyio~=4.8.0
h11~=0.14.0
click~=8.1.8
annotated-types~=0.7.0
sniffio~=1.3.1
exceptiongroup~=1.2.2
python-multipart~=0.0.20
websockets~=14.2
